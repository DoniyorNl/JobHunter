# Prisma CLI + pnpm: hoist Prisma-related packages so postinstall can resolve peers
# (fixes intermittent "Cannot find module 'pathe'" during `prisma generate`)
public-hoist-pattern[]=*@prisma/*
public-hoist-pattern[]=pathe
